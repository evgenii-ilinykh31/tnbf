{
  "name": "tnbf",
  "private": true,
  "workspaces": [
    "packages/*",
    "nextjs"
  ],
  "scripts": {
    "dev": "npm run -w nextjs dev",
    "build": "npm run -w @tnbf/db prisma:generate && npm run -w nextjs build",
    "start": "npm run -w nextjs start",
    "lint": "npm run -w nextjs lint",
    "typecheck": "npm run -w nextjs type-check || tsc -b packages/*"
  },
  "engines": {
    "node": ">=20"
  }
}
