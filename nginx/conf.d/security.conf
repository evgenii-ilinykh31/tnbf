# Базовые заголовки безопасности
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header X-XSS-Protection "1; mode=block" always;

# HSTS — включай, когда убедишься, что HTTPS стабилен
# Если включаешь — не забудь про поддомены и, возможно, preload
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

# Ограничение размеров тела (подстрой под upload)
#client_max_body_size 10m;

# (Необязательно) Современные TLS-шифры — можно добавить при необходимости:
# ssl_protocols TLSv1.2 TLSv1.3;
# ssl_ciphers 'ECDHE-ECDSA-AES256-GCM-SHA384:...';
# ssl_prefer_server_ciphers on;
